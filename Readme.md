# PDF-Chat-RAG

A brief description of what your project does goes here.

## 📁 Folder Structure
This repository contains two main directories:
- `/client`: The frontend application (describe tech stack, e.g., Next.js, React).
- `/server`: The backend API and logic (describe tech stack, e.g., Node.js, Express, Python).

## 🚀 How to Run the Project locally

You will need to open two separate terminals to run the client and server at the same time.

### 1. Start the Backend (Server)
Navigate to the server directory, install dependencies, and start the server.
```bash
cd server
npm install
npm run dev  # (Change this to your actual start command)

cd client
npm install
npm run dev  # (Change this to your actual start command)