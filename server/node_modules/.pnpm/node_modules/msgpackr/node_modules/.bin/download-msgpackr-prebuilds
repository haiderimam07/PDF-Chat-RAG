#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../msgpackr-extract/bin/download-prebuilds.js" "$@"
else
  exec node  "$basedir/../../../msgpackr-extract/bin/download-prebuilds.js" "$@"
fi
# cmd-shim-target=C:/Users/haide/Work/pdf-chat-rag/server/node_modules/.pnpm/msgpackr@2.0.1/node_modules/msgpackr-extract/bin/download-prebuilds.js
